<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <title>Edit Person</title>
</head>
<body>
  <h1>Edit Person</h1>
  <form action="/person/edit/<%= person._id %>" method="POST">
    <label for="name">Name:</label>
    <input type="text" name="name" id="name" value="<%= person.name %>" required>
    
    <label for="age">Age:</label>
    <input type="number" name="age" id="age" value="<%= person.age %>" required>
    
    <label for="gender">Gender:</label>
    <select name="gender" id="gender" required>
      <option value="M" <%= person.gender === 'M' ? 'selected' : '' %>>Male</option>
      <option value="F" <%= person.gender === 'F' ? 'selected' : '' %>>Female</option>
    </select>
    
    <label for="mobile">Mobile:</label>
    <input type="text" name="mobile" id="mobile" value="<%= person.mobile %>" required>
    
    <button type="submit">Save Changes</button>
  </form>
</body>
</html>
